import * as actionTypes from '../actionTypes/actionTypes.index';
import updateObject from '../helpers/updateObject';

const initialState = {
    lessonsInGroupForMonth: [],
    loading: false,
    error: null
}